<% if item.errors.any? %>
  <div class="alert alert-error">
    <h5>Submission failed:</h5>
    <ul>
      <% item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>